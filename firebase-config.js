// STREET SURVIVAL Firebase Config
// 本番Firebase接続ON

window.STREET_SURVIVAL_FIREBASE_ENABLED = true;

window.STREET_SURVIVAL_FIREBASE_CONFIG = {
  apiKey: "ここに運営側と同じ値",
  authDomain: "ここに運営側と同じ値",
  databaseURL: "ここに運営側と同じ値",
  projectId: "ここに運営側と同じ値",
  storageBucket: "ここに運営側と同じ値",
  messagingSenderId: "ここに運営側と同じ値",
  appId: "ここに運営側と同じ値"
};

console.log("STREET SURVIVAL: Firebase ON / LIVE MODE");
